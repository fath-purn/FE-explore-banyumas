import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "About US",
};

export default function about() {
    return (
        <div>
            <h1>About</h1>
        </div>
    )
}